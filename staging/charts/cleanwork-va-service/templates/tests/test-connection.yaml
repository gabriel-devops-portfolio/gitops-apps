apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cleanwork-va-service.fullname" . }}-test-connection"
  labels:
    {{- include "cleanwork-va-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cleanwork-va-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
