apiVersion: v1
kind: Service
metadata:
  name: {{ include "uriel-tween-agency-service.fullname" . }}
  labels:
    {{- include "uriel-tween-agency-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uriel-tween-agency-service.selectorLabels" . | nindent 4 }}
