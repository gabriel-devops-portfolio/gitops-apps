application: banksystem-web
clusterConfig:
  namespace: banksystem
replicaCount: 1
image:
  registry: 111111222222.dkr.ecr.eu-west-1.amazonaws.com
  repository: banksystem-web
  tag: ""
  pullPolicy: Always

imagePullSecrets: []

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/banksystem-web-rds-role
  name: banksystem-web

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
# capabilities:
#   drop:
#   - ALL
# readOnlyRootFilesystem: true
# runAsNonRoot: true
# runAsUser: 1000

service:
  type: ClusterIP
  port: 80

containerPort:
  name: http
  port: 5000

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

httpRoute:
  enabled: true
  annotations: {}
  parentRefs:
    - name: platform-gateway
      namespace: gateway-system
      sectionName: http
  hostnames:
    - banksystem.app.pilotgab.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /headers

resources:
  requests:
    memory: "512Mi"
    cpu: "250m"
  limits:
    memory: "1Gi"
    cpu: "500m"

livenessProbe:
  httpGet:
    path: /health
    port: 5000
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /health
    port: 5000
  initialDelaySeconds: 10
  periodSeconds: 5

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

volumes: []
volumeMounts: []

nodeSelector: {}
tolerations: []
affinity: {}

env:
  - name: ASPNETCORE_ENVIRONMENT
    value: "Production"
  - name: RdsAuthentication__UseIamAuthentication
    value: "true"
  - name: RdsAuthentication__RdsEndpoint
    value: "<RDS_ENDPOINT>"
  - name: RdsAuthentication__DbUser
    value: "banksystem_app"
  - name: RdsAuthentication__AwsRegion
    value: "us-east-1"
  - name: RdsAuthentication__FallbackPassword
    valueFrom:
      secretKeyRef:
        name: banksystem-db-credentials
        key: password
