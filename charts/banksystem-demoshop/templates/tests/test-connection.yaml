apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "banksystem-demoshop.fullname" . }}-test-connection"
  labels:
    {{- include "banksystem-demoshop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "banksystem-demoshop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
