apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "void-vote.fullname" . }}-test-connection"
  labels:
    {{- include "void-vote.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "void-vote.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
