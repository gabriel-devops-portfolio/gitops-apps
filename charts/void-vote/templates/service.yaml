apiVersion: v1
kind: Service
metadata:
  name: {{ include "void-vote.fullname" . }}
  labels:
    {{- include "void-vote.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "void-vote.selectorLabels" . | nindent 4 }}
