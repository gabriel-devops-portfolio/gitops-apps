apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uriel-tween-agency-service.fullname" . }}-test-connection"
  labels:
    {{- include "uriel-tween-agency-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uriel-tween-agency-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
