apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coffee-ae-service.fullname" . }}-test-connection"
  labels:
    {{- include "coffee-ae-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coffee-ae-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
