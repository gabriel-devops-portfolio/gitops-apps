apiVersion: v1
kind: Service
metadata:
  name: {{ include "coffee-ae-service.fullname" . }}
  labels:
    {{- include "coffee-ae-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coffee-ae-service.selectorLabels" . | nindent 4 }}
