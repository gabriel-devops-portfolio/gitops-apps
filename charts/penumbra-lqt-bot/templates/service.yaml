apiVersion: v1
kind: Service
metadata:
  name: {{ include "penumbra-lqt-bot.fullname" . }}
  labels:
    {{- include "penumbra-lqt-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.metrics.enabled }}
    - port: 9090
      targetPort: http
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "penumbra-lqt-bot.selectorLabels" . | nindent 4 }}
