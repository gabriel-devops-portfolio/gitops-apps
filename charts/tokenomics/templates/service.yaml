apiVersion: v1
kind: Service
metadata:
  name: {{ include "tokenomics.fullname" . }}
  labels:
    {{- include "tokenomics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tokenomics.selectorLabels" . | nindent 4 }}
