apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tokenomics.fullname" . }}-test-connection"
  labels:
    {{- include "tokenomics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tokenomics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
