apiVersion: v1
kind: Service
metadata:
  name: {{ include "banksystem-web.fullname" . }}
  labels:
    {{- include "banksystem-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "banksystem-web.selectorLabels" . | nindent 4 }}
