apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "banksystem-web.fullname" . }}-test-connection"
  labels:
    {{- include "banksystem-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "banksystem-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
