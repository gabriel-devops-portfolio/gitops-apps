apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "banksystem-centralapi.fullname" . }}-test-connection"
  labels:
    {{- include "banksystem-centralapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "banksystem-centralapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
