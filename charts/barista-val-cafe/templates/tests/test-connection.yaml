apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "barista-val-cafe.fullname" . }}-test-connection"
  labels:
    {{- include "barista-val-cafe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "barista-val-cafe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
